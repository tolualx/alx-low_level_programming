#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @c: the integar to be computed
 * Return:  the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
