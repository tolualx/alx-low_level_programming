#include <stdio.h>

/**
 * main - prints single digits in base 10
 * Return: always 0
 */
int main(void)
{
	int num;

	(num = 0; num < 10; num++)
	putchar(num % 10) = '0';)

	putchar('\n');

	return (0);
}
